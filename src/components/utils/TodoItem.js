import { StyleSheet } from "react-native";

const StylingTodoItem = StyleSheet.create({
    container: {
        padding: 10,
        borderBottomWidth: 1,
        borderBottomColor: '#ccc',
        
    },
});

export default StylingTodoItem;